<Window x:Class="stalkerdesk.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="StalkerDesk - Admin Panel" Height="600" Width="1000"
        WindowStartupLocation="CenterScreen" Background="#1E1E1E">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Background="#252526" Grid.Column="0" CornerRadius="0,15,15,0">
            <StackPanel Margin="10">
                <TextBlock Text="STALKER DESK" Foreground="White" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,20,0,40"/>

                <Button Content="Dashboard" Style="{StaticResource MenuButtonStyle}"/>
                <Button Content="Komputery" Style="{StaticResource MenuButtonStyle}"/>
                <Button Content="Harmonogram" Style="{StaticResource MenuButtonStyle}"/>
                <Button Content="Ustawienia" Style="{StaticResource MenuButtonStyle}" Margin="0,200,0,0"/>
            </StackPanel>
        </Border>

        <Grid Grid.Column="1" Margin="25">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Zarządzanie pracownią" Foreground="White" FontSize="28" FontWeight="SemiBold" Grid.Row="0"/>

            <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,20">
                <Border Width="150" Height="80" Background="#2D2D30" CornerRadius="10" Margin="0,0,15,0">
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <TextBlock Text="Aktywne" Foreground="#888888"/>
                        <TextBlock Text="12 / 20" Foreground="White" FontSize="20" FontWeight="Bold"/>
                    </StackPanel>
                </Border>
                <Border Width="150" Height="80" Background="#2D2D30" CornerRadius="10">
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <TextBlock Text="Status sieci" Foreground="#888888"/>
                        <TextBlock Text="Stabilna" Foreground="#4EC9B0" FontSize="20" FontWeight="Bold"/>
                    </StackPanel>
                </Border>
            </StackPanel>

            <Border Grid.Row="2" Background="#2D2D30" CornerRadius="15" Padding="15">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="Lista stacji roboczych" Foreground="White" FontWeight="Bold" Margin="0,0,0,10"/>

                    <DataGrid Grid.Row="1" Background="Transparent" BorderThickness="0" Foreground="White" 
                              RowBackground="#333337" AlternatingRowBackground="#3E3E42" 
                              AutoGenerateColumns="False" CanUserAddRows="False" GridLinesVisibility="None">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Nazwa" Binding="{Binding Name}" Width="*"/>
                            <DataGridTextColumn Header="Adres IP" Binding="{Binding IP}" Width="*"/>
                            <DataGridTextColumn Header="Pozostały czas" Binding="{Binding TimeLeft}" Width="*"/>
                            <DataGridTemplateColumn Header="Akcja" Width="120">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="Ustaw czas" Background="#007ACC" Foreground="White" Padding="5,2"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>
        </Grid>
    </Grid>

    <Window.Resources>
        <Style x:Key="MenuButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="Height" Value="45"/>
            <Setter Property="Margin" Value="0,5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#3E3E42"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
</Window>